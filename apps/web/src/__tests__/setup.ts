/**
 * Vitest setup — shared globals for the test environment.
 *
 * Minimal scaffolding after the OpenClaw-gateway removal. Re-add mocks here
 * if a future test ever needs a polyfill or global stub.
 */

import "@testing-library/jest-dom/vitest";
